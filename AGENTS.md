# AGENTS.md

このリポジトリで作業するコーディングエージェント向けの案内。`CLAUDE.md` はこのファイルへのシンボリックリンクなので、編集する場合は `AGENTS.md` を変更する。

## このリポジトリについて

keitakn（開発責任者）の職務経歴書・スキルシートを管理するリポジトリ。

実体は `README.md` 1ファイル（約1,300行）のみで、ソースコードもビルド・テスト・lint の仕組みも存在しない。CI 設定（`.github/`）も無い。
したがって「ビルドする」「テストを流す」といった作業は無く、作業内容は基本的に `README.md` の文章の追記・修正になる。

内容はすべて日本語。読み手は採用担当者・取引先なので、公開文書として自然な日本語で書く。

## README.md の構成

見出しレベルは階層構造どおりに付ける。`# cv` がドキュメントタイトルで、H1 はこの1つだけ。大セクションはすべて `##` で、レベルを飛ばさない。上から順に以下のセクションが並ぶ。

1. 冒頭プロフィール — `# cv` 直下の数段落。得意領域と直近の仕事内容の要約
2. `## 目次` — `##` と `###` の見出しへのアンカーリンク一覧（`####` は載せない）
3. `## 基本情報` — 名前・連絡先・各種アカウントのMarkdown表
4. `## 提供できること` — 依頼できる仕事の箇条書き。各項目に職務経歴で裏付けられる実績をぶら下げる
5. `## 得意な事` / `## あまり得意じゃない事`
6. `## エンジニアリングで意識している事` — フロントエンド / バックエンド / クラウドの3観点を `###` で
7. `## スキルセット` — `### 今メインで使っている技術`（名前と備考のMarkdown表。スキルレベルの列は置かない）と `### 過去に経験した技術`（文章）の2つ。過去に使った技術は表に行を足さず文章側にまとめる
8. `## 今興味がある技術` — 技術ごとに `###` 見出し + 数段落
9. `## 主な職務経歴` — 案件ごとに `###` 見出し

### 職務経歴エントリの書式

**新しい案件ほど上**（`## 主な職務経歴` の直下が最新、末尾が2013年の案件）という逆時系列で並ぶ。新しい経歴を追加するときはセクション先頭に挿入する。

各案件は次の形式で統一されている。

```markdown
### <案件名>（<期間>）

#### 業務概要

#### 利用した技術

#### プロジェクトの規模

#### 自分の役割

#### 主な成果
```

- 見出しは必ずこの順序・この文言。`#### 自分の役割` が省略されている古いエントリもあるが、新規追加では書く。
- 期間の表記は年代によって揺れる（`（2024年-10月〜2024年12月）` / `（2021）` / `（2013年）`）。新規追加は直近のエントリに合わせて `（YYYY年-M月〜YYYY年M月）` 形式にする。
- `#### 利用した技術` は箇条書き。バージョン（`Next.js 15系`、`Python 3.13系`）や用途の補足（`Cloud Run（APIサーバーとして利用）`）を添えるのが慣例。
- `#### プロジェクトの規模` は構成人数と合計人数を書く。
- `#### 主な成果` は箇条書きまたは文章。学びや振り返りを最後に添えることが多い。
- 守秘のため、社名を出さない案件は `某大手ECサイト`、`クレジットカード会社の〜` のようにぼかして書く。社名を明記するかどうかは既存エントリの流儀に従い、判断が必要なら確認する。

## 開発フロー

GitHub Issue 起点で運用されている。

1. Issue を立てる
2. `feature/issue<Issue番号>` ブランチを `main` から切る（例: `feature/issue46`）。Issue に紐づかない場合は `feature/<内容>`、`bugfix/<内容>` も使われる
3. コミットメッセージは日本語。Issue に紐づく場合は先頭に `#<Issue番号>` を付ける（例: `#42 2024年10月以降の業務経歴を追加`）。文章の追記・修正には gitmoji の `:memo:` を付けることが多い（例: `:memo: #39 2024年９月までの新しい業務内容を追加`）
4. `main` へ Pull Request を出してマージ（マージコミットが残る運用）

`main` へ直接コミットしない。

## 更新時に併せて確認すること

職務経歴を追加・更新したときは、内容が古くなりやすい以下も合わせて見直す。

- `## 目次`（`##` / `###` 見出しを追加・変更・削除したらリンクを追随させる。アンカーは GitHub の自動生成 slug（github-slugger）に合わせる。`（）` や `〜` などの記号は除去、英字は小文字化、半角スペースは `-` になる）
- `## 提供できること`（新しい実績で裏付けが更新できないか）
- `## スキルセット` の `### 今メインで使っている技術` の表と `### 過去に経験した技術` の文章（新しく使った技術の追加）
- `## 今興味がある技術`（記述が数年前のまま残りやすい）
